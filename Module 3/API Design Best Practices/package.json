{
  "name": "api-design-best-practices",
  "version": "1.0.0",
  "description": "This API has become expensive in quiet way: not broken enough to force a rewrite, but inconsistent enough to slow every client team that touches it.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
